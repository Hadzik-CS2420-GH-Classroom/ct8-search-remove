<svg xmlns="http://www.w3.org/2000/svg" width="600" height="120" viewBox="0 0 600 120">
  <!-- PLACEHOLDER — replace with final diagram in design tool -->
  <!-- Shows: single 'current' pointer walking head→nullptr        -->
  <!--   State 1: current at a non-matching node (arrow advancing) -->
  <!--   State 2: current at the matching node (highlight/found)   -->
  <!--   State 3 (optional): current at nullptr (not found)        -->
  <rect width="600" height="120" fill="#f5f5f5" rx="6"/>
  <text x="300" y="52" font-family="monospace" font-size="14" fill="#888" text-anchor="middle">contains() — PLACEHOLDER</text>
  <text x="300" y="74" font-family="monospace" font-size="11" fill="#aaa" text-anchor="middle">single pointer traversal · returns true on match, false at nullptr</text>
</svg>
