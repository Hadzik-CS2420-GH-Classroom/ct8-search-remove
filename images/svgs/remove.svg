<svg xmlns="http://www.w3.org/2000/svg" width="600" height="160" viewBox="0 0 600 160">
  <!-- PLACEHOLDER — replace with final diagram in design tool    -->
  <!-- Shows: trailing pointer pair + bypass surgery (middle case) -->
  <!--   Step 1: previous at node A, current at target node B      -->
  <!--   Step 2: previous->next redirected to B->next (skip B)     -->
  <!--   Step 3: delete B — node removed, list intact              -->
  <!-- (head case is delegated to pop_front — no diagram needed)   -->
  <rect width="600" height="160" fill="#f5f5f5" rx="6"/>
  <text x="300" y="68" font-family="monospace" font-size="14" fill="#888" text-anchor="middle">remove() — PLACEHOLDER</text>
  <text x="300" y="90" font-family="monospace" font-size="11" fill="#aaa" text-anchor="middle">trailing pointer bypass · previous-&gt;next skips deleted node</text>
  <text x="300" y="108" font-family="monospace" font-size="11" fill="#aaa" text-anchor="middle">middle/tail case · head case delegates to pop_front()</text>
</svg>
